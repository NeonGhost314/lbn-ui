import React from "react";
import ReactDOM from "react-dom/client";
import LBNApp from "./LBNApp";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <LBNApp />
  </React.StrictMode>
);
